# Dependencies
/node_modules

# Build output
/dist

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# OS generated files
.DS_Store
.AppleDouble
.LSOverride

# Editor directories and files
.idea
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.sublime-workspace

# Optional: Environment files (if you ever use them for local testing)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Optional: Coverage directory
/coverage

# Optional: Lockfiles if you prefer not to commit them (though usually committed)
# package-lock.json
# yarn.lock
